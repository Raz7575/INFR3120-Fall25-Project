<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Report Issue | Campus Issue Tracker</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <header class="site-header">
    <div class="logo-wrap">
      <div class="logo-circle">OT</div>
      <div class="logo-text">
        <h1>Campus Issue Tracker</h1>
        <p class="tagline">Team OT Track</p>
      </div>
    </div>
    <nav class="main-nav">
      <a href="/" class="nav-link">Home</a>
      <a href="/issues/create" class="nav-link active">Report Issue</a>
    </nav>
  </header>

  <main class="container">
    <section class="form-section">
      <h2>Report a New Issue</h2>
      <p class="form-desc">
        Fill out the details below to log a facility issue on campus.
      </p>

      <form action="/issues" method="POST" class="form-card">
        <div class="form-group">
          <label for="title">Issue Title</label>
          <input
            type="text"
            id="title"
            name="title"
            required
            placeholder="e.g., Broken light in hallway"
          />
        </div>

        <div class="form-group">
          <label for="description">Description</label>
          <textarea
            id="description"
            name="description"
            rows="4"
            required
            placeholder="Describe what is wrong and any important details."
          ></textarea>
        </div>

        <div class="form-group">
          <label for="location">Location</label>
          <input
            type="text"
            id="location"
            name="location"
            required
            placeholder="e.g., Science Building, Room 204"
          />
        </div>

        <div class="form-group">
          <label for="priority">Priority</label>
          <select id="priority" name="priority" required>
            <option value="">Select priority</option>
            <option value="Low">Low</option>
            <option value="Medium">Medium</option>
            <option value="High">High</option>
          </select>
        </div>

        <!-- Status can default to "Open" in the backend/model -->
        <div class="form-actions">
          <a href="/" class="btn btn-outline">Cancel</a>
          <button type="submit" class="btn btn-primary">Submit Issue</button>
        </div>
      </form>
    </section>
  </main>

  <footer class="site-footer">
    <p>© <%= new Date().getFullYear() %> OT Track — Campus Issue Tracker</p>
  </footer>
</body>
</html>
